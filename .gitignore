# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# Editors / IDEs
.idea/
.vscode/
.claude/

# Local-only docs
*.docx

# Runtime artifacts
sync_log.txt
*.png
output/

# Documentation images (committed even though *.png is ignored)
!docs/*.png

# Local credentials override (config.json is checked in with placeholders;
# real credentials go in config.local.json, which config_loader.py merges
# on top of config.json at load time)
config.local.json
